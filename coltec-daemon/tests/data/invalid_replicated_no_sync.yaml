# Invalid: persistence.mode='replicated' but no sync paths defined
name: bad-workspace
metadata:
  org: test
  project: test
devcontainer:
  template:
    name: python
    path: templates/python.json
  image:
    name: ghcr.io/foo/bar
persistence:
  enabled: true
  mode: replicated
  # ERROR: no sync paths or multi_scope_volumes.environment defined
